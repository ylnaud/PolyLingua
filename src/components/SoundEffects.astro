---

---

<script>
  import { playAchievement } from '../lib/sound';

  // Bandera de módulo: ver comentario equivalente en ProgressTracker.astro.
  let soundEffectsStarted = false;

  function initSoundEffects() {
    if (soundEffectsStarted) return;
    soundEffectsStarted = true;

    document.addEventListener('polylingua-achievement', () => playAchievement());
  }

  initSoundEffects();
  document.addEventListener('astro:page-load', initSoundEffects);
</script>
